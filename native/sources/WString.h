// Native shim: DisplayDrivers' USE_NATIVE_SDL backend already defines a
// source-compatible global String (see lib/DisplayDrivers/src/backends/native_sdl.h).
// This header exists only so `#include <WString.h>` resolves.
#pragma once
